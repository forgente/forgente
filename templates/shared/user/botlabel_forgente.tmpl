{{/* provenance marker for automated accounts; renders nothing for people.
The operator is named on the account's profile, which the name already links to. */}}
{{if .IsTypeBot}}&nbsp;<span class="ui basic label tw-p-1 tw-align-baseline" data-tooltip-content="{{ctx.Locale.Tr "user.bot_desc"}}">{{ctx.Locale.Tr "user.bot"}}</span>{{end}}
